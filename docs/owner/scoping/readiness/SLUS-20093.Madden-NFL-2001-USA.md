# Madden NFL 2001 (USA) — how much of the Madden 09 module works on it

Measured 2026-09-06 with `tools/owner/ea_module_readiness.py` (`ea_module_readiness/v1`), read-only. Source: `SLUS-20093.Madden-NFL-2001-USA.readiness.json`; every number below is copied from it.

The readers are the shipped Madden 09 ones, imported and not re-implemented: `ea_terf` `8140f0e67d0664eb`, `ea_tdb` `849ba5e6a81805d1`, `ea_schl` `302185d06af22ce9`, `mmap_art` `7456e7b2788ba367`, `containers` `e3233c4e55df1abf` (sha256, first 16).

Retail-free: names, counts, digests and schema field names and widths. No member payload, no decoded pixel, no string out of a text bank.

## Identity

| field | value |
|---|---|
| image | `Madden NFL 2001 (USA).bin`, 639,214,800 bytes, 41 files, raw CD 2352-byte sectors |
| boot file / serial | `SLUS_200.93` / **SLUS-20093** |
| boot ELF sha256 | `3f9f32643e1a930d3333121875920276037fa9925b0a1eb360fc04c41bce311b` |
| PCSX2 CRC | `4E7B2C18` |
| `/DATA` files | 29 of 41 files on the disc |
| wall time | 59.7 s (containers 58.8 s, caches 0.0 s) |

## Readiness — what runs unchanged

| lane | reader | total | works unchanged | refused | % | note |
|---|---|---:|---:|---:|---:|---|
| containers | `ea_terf.parse_terf` | 26 | 26 | 0 | 100.0% | every `/DATA` file whose magic is `TERF` |
| members | `ea_terf.decompress_member` | 8,962 | 8,897 | 65 | 99.3% | decoded to the 32-byte classify head |
| TDB databases | `ea_tdb.parse_tdb` | 319 | 0 | 319 | 0.0% | 0 table(s), 0 field definition(s) |
| CRC sites | `ea_tdb.crc_sites` | 0 | 0 | 0 | — | 0 database(s) whose extents the reader would not guess at |
| MMAP images | `mmap_art.decode_rgba` | 256 | 155 | 101 | 60.5% | sample of 1,639 member(s) with an MMAP head |
| SCHl members | `ea_schl.parse_stream_header` | 4,105 | 4,075 | 30 | 99.3% | one header per member; 4,105 of 4,105 header(s) parsed; 50 stream(s) walked in 36 sampled member(s) |
| BNKl banks | `ea_schl.parse_bank` | 252 | 252 | 0 | 100.0% | 669 sound(s) in their directories |
| preload caches | `containers.parse_preload_cache` | 0 | 0 | 0 | — | `QL01` files on the disc |
| cache copies | byte-compare vs the container | 0 | 0 | 0 | — | 0 unresolved |

Definitions, so a percentage is not a decoration: *containers* is `parse_terf` returning; *members* is the stored bytes decoding as far as the 32-byte classify head, i.e. the codec is one of the three the module implements; *TDB databases* is `parse_tdb` returning; *CRC sites* is the stored CRC-32/MPEG-2 equalling the one recomputed from the file's own bytes; *MMAP images* is `decode_rgba` returning pixels for image 0 of a **sampled** member (the sample size is in the note, and the pixels are measured and dropped); *SCHl streams* is the header parsing **and** the codec being one the audio lane decodes; *BNKl banks* is `parse_bank` returning a directory; *preload caches* is `parse_preload_cache` returning, and *cache copies* is each copy being byte-identical to the container bytes it copies.

### Container shapes

| what | measured |
|---|---|
| chunk chains | TERF -> DIR1 -> DATA 23, TERF -> DIR1 -> COMP -> DATA 2, TERF -> HSH1 -> DIR1 -> DATA 1 |
| member alignments | 64 20, 16 2, 2048 2, 4 2 |
| codecs across every member directory | NONE (stored) 8,584, RLE1 313, LZM1 65 |
| member formats after decompression | SCHl 4,105, unclassified 2,277, MMAP 1,639, TDB 319, empty 260, BNKl 252, SMF 26, TERF 19 |
| decoded texture sizes (sample) | 128x128 58, 32x32 22, 64x64 15, 16x16 8, 640x448 7, 128x16 4, 256x32 4, 64x16 4 |
| SCHl codecs (first stream of each member) | EA-XA ADPCM 4,075, MicroTalk (EA speech, ~10:1) 30 |
| SCHl platform tags | PT 4,105 |
| why a sampled texture would not draw | stored under EA codec # (IPU#), the PlayStation # IPU's own format; nothing here decodes it 48, pixel layout # is not one this decoder reads 5 |

## What a module for this disc would need

One row per studio page. *Container* is the disc's own file whose name the mapper's glossary maps to that page — and, where the glossary (which is Madden 09's naming) names none, whichever containers carry the page's format, marked *(by format, not by name)*. *Feeding formats* are counted after decompression, across the whole disc, by the module's own `identify_member`. The number in brackets is that container's member count in the page's formats.

| page | container(s) on this disc | feeding format(s) present | what the readers do with them |
|---|---|---|---|
| Uniforms & Equipment | `UIS_MCFL.DAT` (1,326), `UIS_TMHL.DAT` (96), `UIS_TMLO.DAT` (91), `UIS_FE.DAT` (62) and 3 more *(by format, not by name)* | MMAP 1,639 | `decode_rgba` draws 155 of 256 sampled texture(s) |
| Names, Numbers & Faces | `LEAGUE.DAT` (313), `TEMPLATE.DAT` (6) | TDB 319, MMAP 1,639 | `parse_tdb` opens 0 of 319 database(s); 0 of 0 CRC sites agree; `decode_rgba` draws 155 of 256 sampled texture(s) |
| Text & Team Identity | `LEAGUE.DAT` (313), `UIS_FONT.DAT` | TDB 319 | `parse_tdb` opens 0 of 319 database(s); 0 of 0 CRC sites agree |
| Field Art & Create-Team Art | `UIS_TMLO.DAT` (91), `UIS_SLIV.DAT`, `UIS_TMFN.DAT` | MMAP 1,639 | `decode_rgba` draws 155 of 256 sampled texture(s) |
| Stadiums | `UIS_MCFL.DAT` (1,326), `UIS_TMHL.DAT` (96), `UIS_TMLO.DAT` (91), `UIS_FE.DAT` (62) and 4 more *(by format, not by name)* | MMAP 1,639, SMF 26 | `decode_rgba` draws 155 of 256 sampled texture(s); 26 SMF member(s): no reader in this repository |
| Presentation | `LOADDATA.DAT` (9), `MOVIEDAT.DAT` | MMAP 1,639 | `decode_rgba` draws 155 of 256 sampled texture(s) |
| Menus & UI | `UIS_MCFL.DAT` (1,326), `UIS_TMHL.DAT` (96), `UIS_TMLO.DAT` (91), `UIS_FE.DAT` (62) and 17 more | MMAP 1,639 | `decode_rgba` draws 155 of 256 sampled texture(s) |
| The Crib | — | — | an ESPN NFL 2K5 feature; a Madden or NCAA disc has no data for it |
| Audio | `SPCHDATA.DAT` (4,033) | SCHl 4,105, BNKl 252 | 4,075 of 4,105 SCHl member(s) carry a codec the audio lane decodes; `parse_bank` opens 252 of 252 bank(s) |
| Gameplay | — | — | the boot ELF `SLUS_200.93` opens: PCSX2 CRC `4E7B2C18`, 1 program header(s), sha256 `3f9f32643e1a930d`. Every patch site is per-title research; nothing here is shared with Madden 09. |
| Playbooks & Plays | `LEAGUE.DAT` (313), `TEMPLATE.DAT` (6) *(by format, not by name)* | TDB 319 | `parse_tdb` opens 0 of 319 database(s); 0 of 0 CRC sites agree |
| All Textures | `UIS_MCFL.DAT` (1,326), `UIS_TMHL.DAT` (96), `UIS_FE.DAT` (62), `UIS_IG.DAT` (29) and 11 more | MMAP 1,639 | `decode_rgba` draws 155 of 256 sampled texture(s) |
| Saves | — | — | not on the disc; a memory-card save is a different source |
| Build & Share | — | — | the shell's own page; no game writes it |

### Do the databases have Madden 09's schema?

Field names and bit widths from each disc's own field directory, dominant shape per table name. `identical` means the roster and identity lanes port with a schema table only; a width difference means the compiler's metadata-driven read still works and any hard-coded offset does not.

| table | on this disc | on Madden 09 | verdict |
|---|---:|---:|---|
| `TEAM` | — field(s) | 65 field(s) | absent here |
| `PLAY` | — field(s) | 110 field(s) | absent here |
| `DCHT` | — field(s) | 4 field(s) | absent here |
| `INJY` | — field(s) | 5 field(s) | absent here |
| `COCH` | — field(s) | 68 field(s) | absent here |
| `SEAI` | — field(s) | 19 field(s) | absent here |
| `SLRI` | — field(s) | 9 field(s) | absent here |
| `FORM` | — field(s) | 3 field(s) | absent here |
| `PBFM` | — field(s) | 9 field(s) | absent here |
| `PBST` | — field(s) | 5 field(s) | absent here |
| `SETL` | — field(s) | 8 field(s) | absent here |
| `PBPL` | — field(s) | 6 field(s) | absent here |
| `PLYL` | — field(s) | 10 field(s) | absent here |

### The preload caches

**No `QL01` preload cache is on this disc.** Every writer's cache-coherence step — the one that keeps a container's cached directory copies in step with the container — has nothing to keep in step here, which removes work rather than adding it. Whether this engine year preloads through some other file is not established by this measurement.

## What the readers refused, grouped by sentence

485 refusal(s) in 14 group(s). Numbers inside a sentence are blanked to `#` so one refusal seen many times is one row.

| reader | refusals | sentence (numbers blanked) | where |
|---|---:|---|---|
| `ea_tdb.parse_tdb` | 248 | table PLAY declares # record(s) of # byte(s), which would end at # in a #-byte file; the file is truncated. | `LEAGUE.DAT:1`, `LEAGUE.DAT:2`, `LEAGUE.DAT:3` |
| `ea_terf.decompress_member` | 65 | member #: member uses codec # (LZM#), which this reader does not implement: it implements # = NONE (stored), # = RLE# # = LZH#. Codecs # (HUFF), # (LZM#) and # (IPU#) are registered by the engine and used by no member of any disc measured.  | `UIS_POPS.DAT` |
| `mmap_art.parse` | 48 | this member declares # surface(s) but puts its surface table at +#x#C, not the +#x# every measured member with surfaces uses. Re-derive the header before trusting it. | `UIS_TMHL.DAT:0`, `UIS_TMHL.DAT:2`, `UIS_TMHL.DAT:4` |
| `mmap_art.undecodable_reason` | 48 | stored under EA codec # (IPU#), the PlayStation # IPU's own format; nothing here decodes it | `UIS_MCFL.DAT:0`, `UIS_MCFL.DAT:27`, `UIS_MCFL.DAT:55` |
| `ea_tdb.parse_tdb` | 32 | table PCDE declares # record(s) of # byte(s), which would end at # in a #-byte file; the file is truncated. | `LEAGUE.DAT:502`, `LEAGUE.DAT:503`, `LEAGUE.DAT:504` |
| `ea_tdb.parse_tdb` | 32 | table TSSE declares # record(s) of # byte(s), which would end at # in a #-byte file; the file is truncated. | `LEAGUE.DAT:534`, `LEAGUE.DAT:535`, `LEAGUE.DAT:536` |
| `mmap_art.undecodable_reason` | 5 | pixel layout # is not one this decoder reads | `UIS_COMN.DAT:13`, `UIS_FE.DAT:21`, `UIS_FE.DAT:24` |
| `ea_tdb.parse_tdb` | 1 | field A\x#\x#\x# of table GOPT covers bits #..# of a record that is # byte(s) long; the field directory is being read at the wrong offset or the file is damaged. | `TEMPLATE.DAT:10` |
| `ea_tdb.parse_tdb` | 1 | table AGCM declares # record(s) of # byte(s), which would end at # in a #-byte file; the file is truncated. | `TEMPLATE.DAT:1` |
| `ea_tdb.parse_tdb` | 1 | table COCH declares # record(s) of # byte(s), which would end at # in a #-byte file; the file is truncated. | `TEMPLATE.DAT:0` |
| `ea_tdb.parse_tdb` | 1 | table LEAG declares # record(s) of # byte(s), which would end at # in a #-byte file; the file is truncated. | `LEAGUE.DAT:0` |
| `ea_tdb.parse_tdb` | 1 | table PSUA declares # record(s) of # byte(s), which would end at # in a #-byte file; the file is truncated. | `TEMPLATE.DAT:11` |
| `ea_tdb.parse_tdb` | 1 | table SCHD declares # record(s) of # byte(s), which would end at # in a #-byte file; the file is truncated. | `TEMPLATE.DAT:2` |
| `ea_tdb.parse_tdb` | 1 | the field at offset # is named '..', which carries no printable character at all; the directory is being read at the wrong offset or the file is damaged. | `TEMPLATE.DAT:12` |

